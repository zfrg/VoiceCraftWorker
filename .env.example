# 前后端已一体化部署到同一 Cloudflare Worker（单一项目，见官方 vite-react-template 结构）。
# 生产环境：API 与静态资源同域，无需配置 VITE_API_BASE（留空即可）。
# 本地开发：`npm run dev` 用 @cloudflare/vite-plugin 同时启动前端与 Worker（同源端口 5173），
# /v1 请求自动路由到本地 Worker，同样无需 VITE_API_BASE。
# 如需前端单独直连某个 Worker 地址，可在此覆盖：
# VITE_API_BASE=https://your-worker.example.workers.dev